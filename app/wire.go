//go:build wireinject

package app

// This file marks the intended composition root for Google Wire.
// The generated constructor currently lives in wire_gen.go so the
// refactor remains buildable before adding wire generation to the toolchain.
