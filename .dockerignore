/recordings
main
main.exe
mediasink.sqlite3
*.md
log.sh
test.sh
ffmpeg
bin
Dockerfile*
docker-compose*
.dockerignore
.git
.gitignore
LICENSE
.vscode
.idea
.claude
.gemini
.runtime
frontend/node_modules
cli
mobile

# Model export tooling (./server/install-model.sh). The virtualenv alone is ~1.2 GB
# of build context; the exported model is committed, so none of this is needed.
/server/.venv-export
/server/scripts
**/__pycache__
**/*.pyc

# Locally installed ONNX runtime — the image builds its own in the onnx_builder stage
/server/lib

# ONNX external-weight sidecars; the committed model is self-contained
**/*.onnx.data